import React from "react";
import "./SignUp.css";

export const SignUpBtn = (props) => {
    return (
        <button className="uk-button uk-button-default btn" {...props}>Sign Up
        </button>
    )
};