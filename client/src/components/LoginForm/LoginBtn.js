import React from "react";
import "./Login.css";

export const LoginBtn = (props) => {
    return (
        <button className="uk-button uk-button-default btn" {...props}>Sign In
        </button>
    )
};